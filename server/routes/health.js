const express = require('express');
const router = express.Router();

// Placeholder route - to be implemented
router.get('/', (req, res) => {
  res.json({ message: 'Health API - Coming Soon' });
});

module.exports = router;
